# Local compiler and native build products.
.build/
build/*
!build/.gitkeep

# The prior game is kept locally as read-only implementation reference.
IceEscapeReference/
JaiStd/

# Local browser runtime harness dependencies and output.
node_modules/
web/runtime-check.png
